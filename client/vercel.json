{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rootDirectory": "client",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "http://localhost:6000/api/$1"
    }
  ]
}